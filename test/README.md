# E2E Manual Tests

Run `test/e2e-walking-skeleton.md` once per Phase 1 release gate. Fill the sign-off table, note any scaffolded App X `electron-41.2.x.tgz` 404 workaround by pinning exact `41.2.1` in `package.json`, paste the completed markdown into the Phase 1 verification PR, and use [`test/scripts/clean-slate-machine-a.sh`](scripts/clean-slate-machine-a.sh) on MACHINE A to remove local artifacts before rerunning the checklist.
